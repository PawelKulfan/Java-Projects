package variousExcersises;

public class ObjectConstructor {/*
public Burger(String size, double price) {//spersonalizowany konstruktor obiektów, nazwa taka sama jak klasa (wtedy zastępi defaultowy)
    this.size = size;//ustawienie atrybutów przesłanych w wywołaniu metody
    this.price = price;
    Burger small = new Burger("small", 25.21);//utworzenie instancji obiektu z przesłaniem parametrów
*/}
